
@using PollsApp.Mvc.ViewModels

@model PollsViewModel

<div class="comments-container" id="comments-container">

    <partial name="_CommentsInput" />

    <div class="comments-list" id="comments-list">
        @foreach (var comment in Model.PollInfo.Poll.Comments)
        {
            <div class="comment">
                <img class="comment-avatar-img" src="data:image/jpeg;base64,@Convert.ToBase64String(@comment.User.Avatar)" alt="img" />
                <p>@comment.User.Name</p>
                <p>@comment.Text</p>
            </div>

        }
    </div>
    
</div>